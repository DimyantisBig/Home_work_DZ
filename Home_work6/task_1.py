import random
num_list = []
while len(num_list) < 10:
    num = random.randint(1,100)
    num_list.append(num)
max_num = max(num_list)
print(max_num)

"""
Импорт модуля random:

Ты импортируешь модуль random, чтобы использовать его для генерации случайных чисел.
Создание пустого списка:

num_list = [] — ты создаешь пустой список, в который будут добавляться случайные числа.
Цикл для генерации случайных чисел:

while len(num_list) < 10: — этот цикл будет работать до тех пор, пока в списке не будет 10 чисел.
Внутри цикла:
num = random.randint(1, 100) — ты генерируешь случайное число от 1 до 100.
num_list.append(num) — добавляешь сгенерированное число в список.
Поиск максимального числа:

max_num = max(num_list) — здесь ты используешь встроенную функцию max(), которая находит наибольшее число в списке num_list. Это удобно и эффективно, так как тебе не нужно писать собственный цикл для поиска максимума.
Вывод максимального числа:

print(max_num) — выводишь наибольшее число на экран.
"""






